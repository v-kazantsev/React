import React from 'react';
import RouterCode from '../Code/Router';

const Home = () => (
  <div>
    Use links to navigate between pages.
    <div><RouterCode /></div>
  </div>
)

export default Home;
